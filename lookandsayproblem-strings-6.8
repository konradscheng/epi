string countAndSay(int n) {
    string curr = "1";
    string next = "";
    
    char currentChar;
    int numChars;
    
    for (int i = 1; i < n; i++) {
        
        currentChar = curr[0];
        numChars = 1;
        
        for (int j = 1; j < curr.length(); j++) {
            
            if (currentChar == curr[j]) {
                numChars++;
            } else {
                
                next += to_string(numChars);
                next += currentChar;
                
                currentChar = curr[j];
                numChars = 1;
            }
            
        }
        
        cout << curr << endl;
        next += to_string(numChars);
        next += currentChar;
        curr = next;
        next = "";
    }
    
    return curr;
}