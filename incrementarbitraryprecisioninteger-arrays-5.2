vector<int> PlusOne(vector<int> a) {

	int carry = 1;

	for (int i = a.size() -  1; i >= 0; i--) {

		a[i] += carry;
		carry = 0;

		if (a[i] >= 10) {
			carry = 1;
			a[i] -= 10;
		} else {
			return a;
		}
	}

	if (carry) {
		a[0] = 1;
		a.push_back(0);
	}

	return a;
}