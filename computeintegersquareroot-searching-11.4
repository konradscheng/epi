int SquareRoot(int k) {
	int left = 0;
	int right = 0;

	while (left <= right) {
		int mid = left + (right - left) / 2;
		int sq = mid * mid;

		if (sq <= k) {
			left = mid + 1;
		} else {
			right = mid - 1;
		}
	}

	return left - 1;
}