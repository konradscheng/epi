long Reverse(int x) {

	long res = 0;
	int multiplier = 10;

	bool isNeg = false;

	if (x < 0) {
		isNeg = true;
		x *= -1;
	}



	while (x > 0) {
		res = (res * 10) + (x % multiplier);
		x /= 10;
	}

	if (isNeg) {
		res *= -1;
	}
}