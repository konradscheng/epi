TreeNode* FindFirstGreaterThanK(TreeNode* root, int k) {
	TreeNode * res = nullptr;

	while (root) {
		if (root->val > k) {
			res = root;
			root = root->left;
		} else {
			root = root->right;
		}
	}

	return res;
}